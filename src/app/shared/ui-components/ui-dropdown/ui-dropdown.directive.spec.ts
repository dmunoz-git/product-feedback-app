import { UIDropdownDirective } from './ui-dropdown.directive';

describe('UIDropdownDirective', () => {
  it('should create an instance', () => {
    const directive = new UIDropdownDirective();
    expect(directive).toBeTruthy();
  });
});
