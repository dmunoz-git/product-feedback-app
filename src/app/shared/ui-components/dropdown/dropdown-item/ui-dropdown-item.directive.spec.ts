import { UIDropdownItemDirective } from './ui-dropdown-item.directive';

describe('UIDropdownItemDirective', () => {
  it('should create an instance', () => {
    const directive = new UIDropdownItemDirective();
    expect(directive).toBeTruthy();
  });
});
