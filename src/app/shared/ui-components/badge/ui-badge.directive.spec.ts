import { UIBadgeDirective } from './ui-badge.directive';

describe('UIBadgeDirective', () => {
    beforeEach(() => {});
    it('should create an instance', () => {
        const directive = new UIBadgeDirective();
        expect(directive).toBeTruthy();
    });
});
