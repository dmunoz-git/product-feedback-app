import { EmptyFeedbacksBodyDirective } from './empty-feedbacks.directive';

describe('EmptyFeedbacksDirective', () => {
    it('should create an instance', () => {
        const directive = new EmptyFeedbacksBodyDirective();
        expect(directive).toBeTruthy();
    });
});
